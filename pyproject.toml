[project]
name = "annota"
version = "1.0.0"
description = "Annota — AI-Powered Paper Annotation MCP Server"
requires-python = ">=3.10"
dependencies = [
    "mcp",
    "pymupdf>=1.24.0",
]

[project.scripts]
annota = "annota.server:mcp.run"
