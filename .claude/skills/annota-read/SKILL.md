---
name: annota-read
description: >
  论文全套深度阅读。一个命令完成：交互式确认阅读深度 → 四色全覆盖标注 → 结构化笔记 → 可选审稿意见。
  这是 Annota 的主入口，适合"帮我读这篇论文"这类开放式请求。
  触发词：read paper、帮我读、阅读论文、深度阅读、read this、帮我分析这篇
when_to_use: >
  用户给出 PDF 路径或论文标题，但没有具体说要做什么；
  或用户说"帮我读这篇论文"、"分析一下这篇"、"深度阅读"等开放式请求。
argument-hint: "[PDF路径或论文标题]"
user-invocable: true
disable-model-invocation: false
effort: high
---

# 论文全套深度阅读

用户给了一篇论文，但没有具体指令。你的目标是给出**开箱惊艳**的阅读体验。

## 输入解析

用户输入：`$ARGUMENTS`

---

## Step 1：快速扫描，生成论文概览

### 1.1 定位论文

如果是文件路径直接使用；否则调用 `search_zotero_items(query)` 搜索。
同时调用 `get_item_metadata(item_id)` 获取元数据。

### 1.2 提取全文

调用 `get_pdf_text_bulk(item_id, skip_refs=True)`。

### 1.3 输出论文快照（在对话中展示，不保存）

阅读全文后，用以下格式输出一段**论文快照**，让用户知道你已经读懂了：

```
📄 **{标题}**
👥 {作者} · {会议/期刊} {年份}

🎯 **一句话核心**：{用一句话说清楚这篇论文做了什么，解决了什么问题}

💡 **3个关键贡献**：
  1. {贡献1}
  2. {贡献2}
  3. {贡献3}

📊 **最亮眼的数据**：{最重要的一两个实验结果数字}

⚠️ **主要局限**：{最关键的一个局限}
```

---

## Step 2：交互式确认阅读方案

展示快照后，**立即询问**用户想要什么深度：

```
我已经读完了这篇论文。接下来你想要：

A) 🚀 **全套深度阅读**（推荐）
   四色标注全文重点 + 结构化阅读笔记保存到 Zotero
   预计创建 15-25 条标注 + 1 份完整笔记

B) 🎨 **只做标注**
   四色高亮：贡献(紫)、方法(蓝)、结果(绿)、不足(红)
   预计 15-25 条标注

C) 📝 **只写笔记**
   生成结构化阅读笔记保存到 Zotero

D) 🔍 **审稿模式**
   以顶会审稿人视角给出评审意见 + 评分

E) 🎯 **自定义**
   告诉我你最关心什么（某个具体方法、某类实验、某个问题）

直接回复 A/B/C/D/E，或者告诉我你想要什么。
```

---

## Step 3A：全套深度阅读（选 A）

### 标注阶段

分析全文，识别以下四类内容，**每类至少找 3-5 处**：

| 类型 | 颜色 | 代码 | 标准 |
|------|------|------|------|
| 核心贡献/创新点 | 紫色 | #a28ae5 | 论文声称的 novelty、contribution |
| 方法/架构/算法 | 蓝色 | #2EA8E5 | 关键技术设计、核心机制 |
| 实验结果/数据 | 绿色 | #28CA42 | 具体数字、性能对比、improvement |
| 局限/不足/future work | 红色 | #ff6666 | limitation、weakness、future work |

输出标注清单（在对话中展示）：
```
将创建以下标注：
[紫] Page 1: "We propose DCC, a novel..."
[蓝] Page 3: "The key insight is..."
[绿] Page 6: "achieves 2.3× speedup over..."
...（共 N 条）

⚠️ 写入需要关闭 Zotero。请确认已关闭后回复"确认"。
```

用户确认后，调用 `batch_annotate` 一次性写入所有标注。

### 笔记阶段

生成完整结构化笔记（HTML 格式），内容比 `/annota-summarize` 更深入：

```html
<h2>📄 论文信息</h2>
<p><b>标题：</b>{title}</p>
<p><b>作者：</b>{authors}</p>
<p><b>发表：</b>{venue} {year}</p>
<p><b>DOI：</b>{doi}</p>

<h2>🎯 核心问题与动机</h2>
<p><b>问题：</b>{这篇论文要解决什么问题？}</p>
<p><b>为什么重要：</b>{这个问题的重要性和现有方案的不足}</p>

<h2>💡 核心贡献（对应紫色标注）</h2>
<ol>
<li><b>{贡献1标题}：</b>{具体说明}</li>
<li><b>{贡献2标题}：</b>{具体说明}</li>
<li><b>{贡献3标题}：</b>{具体说明}</li>
</ol>

<h2>🔧 方法详解（对应蓝色标注）</h2>
<p><b>整体架构：</b>{一段话描述整体方案}</p>
<p><b>关键设计1：</b>{设计决策及其原因}</p>
<p><b>关键设计2：</b>{设计决策及其原因}</p>

<h2>📊 实验结果（对应绿色标注）</h2>
<ul>
<li><b>主要结论：</b>{最重要的实验结论}</li>
<li><b>关键数据：</b>{具体数字，如"比 baseline 快 2.3×"}</li>
<li><b>Baseline 对比：</b>{与哪些方法对比，结果如何}</li>
</ul>

<h2>⚠️ 局限与不足（对应红色标注）</h2>
<ul>
<li><b>论文自述：</b>{作者承认的 limitation}</li>
<li><b>潜在问题：</b>{你认为的其他不足}</li>
</ul>

<h2>💭 批判性思考</h2>
<p><b>最强的地方：</b>{这篇论文最有说服力的点}</p>
<p><b>最弱的地方：</b>{最值得质疑的地方}</p>
<p><b>可复现性：</b>{实验设置是否清晰，能否复现}</p>

<h2>🔗 对我的启发</h2>
<p>{这篇论文对读者自己研究的启发，如果知道用户研究方向则定制}</p>

<h2>📚 值得追读</h2>
<ul>
<li>{引用中最值得读的 2-3 篇相关工作}</li>
</ul>
```

调用 `add_child_note(parent_item_id, note_content)` 保存。

### 完成汇报

```
✅ 全套阅读完成！

📌 标注：创建了 {N} 条标注
  - {N1} 条贡献标注（紫色）
  - {N2} 条方法标注（蓝色）
  - {N3} 条结果标注（绿色）
  - {N4} 条局限标注（红色）

📝 笔记：已保存结构化阅读笔记到 Zotero

请在 Zotero 中按 Ctrl+Shift+R 刷新查看。

还想要什么？可以继续问我：
- "这篇论文的方法和 XXX 有什么区别？"
- "帮我以 MICRO 审稿人视角评审"
- "把第3页的公式解释一下"
```

---

## Step 3B：只做标注（选 B）

执行 Step 3A 的标注阶段，跳过笔记阶段。

---

## Step 3C：只写笔记（选 C）

执行 Step 3A 的笔记阶段，跳过标注阶段。

---

## Step 3D：审稿模式（选 D）

转交给 `/annota-review` 的完整流程执行。

---

## Step 3E：自定义（选 E）

用户说明关注点后，针对性执行：
- 如果关注某个方法细节 → 只标注方法相关内容 + 写方法解析笔记
- 如果关注实验 → 深度分析实验设计，标注所有数据点
- 如果关注与某篇论文的对比 → 提取对比维度，生成对比表格

---

## 通用原则

- **不要等用户说"开始"**：展示快照后立即给出选项，用户回复选项后立即执行
- **标注要有密度**：每类至少 3-5 条，让打开 Zotero 的用户第一眼就能看到满满的标注
- **笔记要有深度**：不是复制原文，而是提炼+批判+启发
- **保持对话**：完成后主动提示用户可以继续追问
